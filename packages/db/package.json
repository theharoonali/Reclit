{
  "name": "@repo/db",
  "private": true,
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "find src -name '*.test.ts' | grep -q . && bun test --exit src || echo 'No tests yet'",
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    "./client": "./src/client.ts",
    "./errors": "./src/errors.ts",
    "./schema": "./src/schema.ts",
    "./utils/health": "./src/utils/health.ts"
  },
  "dependencies": {
    "@repo/logger": "workspace:*",
    "@types/pg": "^8.20.0",
    "drizzle-orm": "^0.45.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  }
}
